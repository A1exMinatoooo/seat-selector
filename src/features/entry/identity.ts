export type IdentityCandidate = { id: string; nameFirst: string; phoneDigits: string; phoneIsFull: boolean };
export type IdentityStep = { status: "name-first" } | { status: "phone-prefix"; prefixLength: number; options: string[] } | { status: "resolved"; participantId: string };
export function resolveIdentity(candidates: IdentityCandidate[], nameFirst?: string, phonePrefix?: string): IdentityStep { if (candidates.length === 1) return { status: "resolved", participantId: candidates[0]!.id }; if (!nameFirst) return { status: "name-first" }; const named = candidates.filter((candidate) => candidate.nameFirst === Array.from(nameFirst)[0]); if (named.length === 1) return { status: "resolved", participantId: named[0]!.id }; if (!named.length) return { status: "name-first" }; for (let length = 3; length <= 11; length += 1) { const options = [...new Set(named.filter((item) => item.phoneIsFull).map((item) => item.phoneDigits.slice(0, length)))]; if (options.length > 1 || length === 11) { if (!phonePrefix) return { status: "phone-prefix", prefixLength: length, options }; const matched = named.filter((candidate) => candidate.phoneDigits.startsWith(phonePrefix)); if (matched.length === 1) return { status: "resolved", participantId: matched[0]!.id }; } } return { status: "name-first" }; }
